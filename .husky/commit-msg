#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Commit message validation
echo "📝 Validating commit message..."

# Change to web directory for commitlint
cd web

# Check commit message format using commitlint
npx commitlint --edit "$1"